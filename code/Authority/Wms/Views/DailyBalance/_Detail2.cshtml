<script type="text/javascript">
    function UnitTypeClick2() {
        var unit_type = $('#unitType2').combo('getValue');
        $('#checkDetails').datagrid({ url: '/DailyBalance/InfoCheck/?WarehouseCode=' + warehouse_Code + '&&SettleDate=' + settle_Date + '&&UnitType=' + unit_type });
        $('#InfoCheck').dialog('open').dialog('setTitle', '日结核对');
    }
    function format1(val, row) {
        if (row.Status1 =="F") {
            return '<span style="color:red;">' + val + '</span>';
        } else {
            return val;
        }
    }
    function format2(val, row) {
        if (row.Status2 == "F") {
            return '<span style="color:red;">' + val + '</span>';
        } else {
            return val;
        }
    }
    function format3(val, row) {
        if (row.Status3 == "F") {
            return '<span style="color:red;">' + val + '</span>';
        } else {
            return val;
        }
    }
    function format4(val, row) {
        if (row.Status4 == "F") {
            return '<span style="color:red;">' + val + '</span>';
        } else {
            return val;
        }
    }
    function format5(val, row) {
        if (row.Status5 == "F") {
            return '<span style="color:red;">' + val + '</span>';
        } else {
            return val;
        }
    }
        function format6(val, row) {
            if (row.Status6 == "F") {
                return '<span style="color:red;">' + val + '</span>';
            } else {
                return val;
            }
    }
</script>
<div id="InfoCheck" class="easyui-dialog" modal="true" style="width: 1000px; height: 500px;
    padding: 2px 2px" closed="true">
    <table id="checkDetails" class="easyui-datagrid" border="false" singleselect="true" pageSize="50"
        fit="true" fitcolumns="true" pagination="true" rownumbers="true" toolbar="#chaxun2">
        <thead>
            <tr>                
               <th field="SettleDate" width="90">
                    结算日期
                </th>
                <th field="WarehouseName" width="120">
                    仓库名称
                </th>
                <th field="ProductCode" width="80">
                    商品代码
                </th>
                <th field="ProductName" width="160">
                    商品名称
                </th>
                <th field="UnitName" width="80">
                    单位
                </th>
                <th field="Beginning" align="center" width="120" formatter="format1">
                    期初量(本/销)
                </th>
                <th field="EntryAmount" align="center" width="120" formatter="format2">
                    入库量(本/销)
                </th>
                <th field="DeliveryAmount" align="center" width="120" formatter="format3">
                    出库量(本/销)
                </th>
                <th field="LossAmount" align="center" width="120" formatter="format4">
                    报损量(本/销)
                </th>
                <th field="ProfitAmount" align="center" width="120" formatter="format5">
                    报益量(本/销)
                </th>
                <th field="Ending" align="center" width="120" formatter="format6">
                    结余量(本/销)
                </th>
            </tr>
        </thead>
    </table>
</div>
<div id="chaxun2" style="padding: 5px; height: auto">
    转换单位:
    <select id="unitType2" class="easyui-combobox" style="width: 130px; height: 40px" editable="false">
        <option value="">请选择</option>                       
        <option value="1">标准件</option>
        <option value="2">标准条</option>
        <option value="3">自然件</option>
        <option value="4">自然条</option>
    </select>
 <a href="#" class="easyui-linkbutton" iconcls="icon-ok" plain="true" onclick="UnitTypeClick2()">查询</a>
    &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
    <span style="color:red;">(备注：表格中的红色单元格，"≠"左边为本系统日结结果，"≠"右边为营销系统日结结果，请注意核对。)</span>
</div>
