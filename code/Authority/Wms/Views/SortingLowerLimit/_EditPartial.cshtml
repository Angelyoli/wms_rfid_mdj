<script type="text/javascript">
function editClick() {
    var row = $('#details').datagrid('getSelected');    
    if (row) {
        $('#dlg').dialog('open').dialog('setTitle', '编辑');
        editKey = true;
        $('#LowerLimitfm').form('load', row);   
        $('#LowerLimitfm .easyui-validatebox').validatebox({
            required: true
        });
        if (row.IsActive == '可用') {
            $('#IsActiveCc').combo('setValue', 1).combo('setText', row.IsActive);
        } else {
            $('#IsActiveCc').combo('setValue', 0).combo('setText', row.IsActive);
        }
        if (row.IsRounding == '不取整') {
            $('#isRounding').combo('setValue', 1).combo('setText', row.IsRounding);
        }
        if (row.IsRounding == '取整件') {
            $('#isRounding').combo('setValue', 0).combo('setText', row.IsRounding);
        }
        if (row.IsRounding == '通道机') {
            $('#isRounding').combo('setValue', 3).combo('setText', row.IsRounding);
        }
        $('#IsActiveCc').combo({
            hasDownArrow: true
        });
        $('form input').removeAttr("disabled");
        $('form textarea').removeAttr("disabled");    
        url = '/SortingLowerLimit/Edit/';
    } else {
        $.messager.alert('错误', '没有选择行！');
    }
};
</script>
   