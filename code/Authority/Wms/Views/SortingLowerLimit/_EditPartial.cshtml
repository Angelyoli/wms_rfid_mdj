<script type="text/javascript">
function editClick() {
    var row = $('#details').datagrid('getSelected');
    if (row) {
        $('#dlg').dialog('open').dialog('setTitle', '编辑');
        editKey = true;
        $('#LowerLimitfm').form('load', row);   
        $('#LowerLimitfm .easyui-validatebox').validatebox({
            required: true
        });
        if (row.IsActive == '可用') {
            $('#IsActiveCc').combo('setValue', 1).combo('setText', row.IsActive);
        } else {
            $('#IsActiveCc').combo('setValue', 0).combo('setText', row.IsActive);
        }
        if (row.SortType == '立式机') {
            $('#isSortType').combo('setValue', 1).combo('setText', row.SortType);
        }
        if (row.SortType == '通道机') {
            $('#isSortType').combo('setValue', 2).combo('setText', row.SortType);
        }
        if (row.SortType == '混合烟道') {
            $('#isSortType').combo('setValue', 0).combo('setText', row.SortType);
        }
        $('#IsActiveCc').combo({
            hasDownArrow: true
        });
        $('form input').removeAttr("disabled");
        $('form textarea').removeAttr("disabled");    
        url = '/SortingLowerLimit/Edit/';
    } else {
        $.messager.alert('错误', '没有选择行！');
    }
};
</script>
   